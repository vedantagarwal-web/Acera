{
  "name": "acera-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ./apps/web dev",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@clerk/nextjs": "^6.23.0"
  }
}
